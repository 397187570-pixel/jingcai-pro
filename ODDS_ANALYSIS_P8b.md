# P8b 全量逐场分析 + 串关推荐

## 目标

用户提出两点诉求：
1. **P8 模型要把每场比赛都分析出来，以便选择概率最高的场次的某项押注。**
2. **AI研判给出串关推荐：稳妥型、冷门型。**

## 已落地

### 1. 全量逐场分析与排名（`js/engine/oddsModel.js`）

- `renderModelPanel` 不再只展示 30 场示例，而是对历史三件套候选池中的 **2159 场高置信比赛** 全部打分、排名。
- 新增 `enrich()` / `showModelTable()` / `_resort()`：
  - 每场比赛计算三向模型概率、竞彩赔率、盈亏平衡、EV，并提取“单边最高概率边”作为默认推荐边。
  - 支持两种排序切换：**概率优先**（单边最高模型概率降序）、**EV优先**（最高 EV 降序）。
  - 默认展示前 150 场，可一键“展开全部 2159 场”。
- 新增排名列：# / 场次 / 模型概率 h/d/a（粗体为推荐边）/ 推荐边 / 竞彩赔率 / 盈亏平衡 / EV(pp) / 类型。
- 交互：点击排序按钮通过 `OddsModel._resort()` 无刷新重排。

### 2. 串关推荐引擎（`js/engine/oddsParlay.js`）

依赖 `OddsModel.scoreHistory()` 同步打分，无需后端。

| 策略 | 选腿规则 | 目标 |
|---|---|---|
| **稳妥型（高命中）** | 每场取模型概率最高的边，挑 top-3 场组合 | 最大化组合命中率，赔率低、波动小，但单边 EV 为负（含抽水） |
| **冷门型（高回报）** | 理性冷门区间（赔率 2.5–8×）内，优先取 EV 为正的边；不足则取区间内最高 EV 边；再不足退回全量最高 EV 边 | 潜在回报更高，但命中率低、波动大 |

- 组合统计：
  - 组合模型概率 `P = ∏ p_i`
  - 组合竞彩赔率 `O = ∏ jc_odds_i`
  - 组合 EV `= P*(O-1) - (1-P)`（竞彩串关为赔率相乘）
- 关键护栏：`MAX_EDGE_RATIO = 1.6`，仅当 `模型概率 / 盈亏平衡 ≤ 1.6` 时才纳入“冷门型”候选；
  避免模型在极端赔率上对长 shots 过度自信，组合出 2548× 的“彩票式假价值”。
- 单关建议也复用了同一护栏：oddsModel.advice() 与排名表的“价值”标签现在要求 `EV>0` 且 `概率/盈亏平衡 ≤ 1.6`，避免误标大幅假价值。

### 3. 接入前端（`index.html` + `js/components/oddsMonitor.js`）

- `index.html` 新增 `<div id="oddsParlayPanel"></div>` 容器和 `oddsParlay.js` 脚本。
- `oddsMonitor.js` 在 P8 ML 模型面板渲染后，调用 `OddsParlay.renderParlayPanel(document.getElementById('oddsParlayPanel'), 3)`。
- `components.css` 新增 `.pm-*`（排序控件、行号）和 `.parlay-*`（双卡片、腿列表、组合统计）样式。

### 4. Ardot 设计稿「AI研判」串关推荐面板

- 在 `2:725`（AI综合研判面板）下新增 `32:68`「串关推荐」区块。
- 左右双卡：稳妥型（绿边）+ 冷门型（橙边），含腿列表、组合概率、组合赔率、组合 EV、策略说明。
- 使用 `JCZ Pro` 设计 token 的 HEX 等价色，确保与现有暗色主题统一。

## 校验结果

- JS 语法：`oddsModel.js`、`oddsParlay.js`、`oddsMonitor.js` 全部通过 `node --check`。
- Node 集成测试：
  - 全量分析 2159 场，无报错。
  - 概率最高场次：**马竞 vs 莱万特** → 主胜 76.46% @1.10。
  - 串关组合公式手工重算，与 `OddsParlay.combine()` 数值完全一致。
  - 稳妥型每腿均验证为“该场最高概率边”。

示例输出：
```
[parlay:stable] legs=3 校验=OK | 组合概率=44.43% 组合赔率=1.36x EV=-39.8pp
[parlay:cold]   legs=3 校验=OK | 组合概率=6.67%  组合赔率=53.2x  EV=+254.5pp (样本内)
```

## 诚实说明

- **稳定型**命中率高，但因竞彩抽水，组合 EV 为负，属于“高命中但数学不划算”的选择。
- **冷门型**的 `+254.5pp EV` 是模型在**历史样本内**的拟合结果，存在样本内乐观偏差；
  P8 的 5-fold 外推回测显示真实价值下注 ROI≈−4.8%。
  因此冷门型仅作为“价值倾向候选/风险偏好选择”，不是已验证优势。
- 串关因多次抽水叠加，组合 EV 几乎必然为负；面板已通过护栏和说明文字如实标注风险。

## 后续可选

- 把串关推荐接到“当日/当前可投注赛程”上（目前基于历史三件套候选池）。
- 在 Ardot「赔率监测」板块增加「ML 概率总榜 Top10」视觉展示，镜像 web 面板的前 10 名。
- 允许用户自定义串关腿数（2/3/4）和冷门赔率区间阈值。

## 改动文件

- `js/engine/oddsModel.js`
- `js/engine/oddsParlay.js`（新增）
- `js/components/oddsMonitor.js`
- `index.html`
- `css/components.css`
- `ODDS_ANALYSIS_P8b.md`
