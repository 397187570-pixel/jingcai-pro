# P5 权重规律挖掘（历史三件套回测）

> 用户诉求：「识别在预测赛果时具有较高权重的关键指标与规律…输出可用于辅助判断的规律模型或权重分析结果」+ 硬约束「必须能辅助购买意向/建议」。

## 一、方法

新建 `js/engine/oddsPatterns.js`，加载 2159 场高置信三件套（竞彩隐含 + 欧盘均价隐含 + 赛果），挖**静态偏差**规律（历史集每场只有一个国际赔率快照，无时序，故挖"竞彩 vs 欧洲共识偏差"而非 P3/P4 的随时间背离）。

**特征**（原子、可解释，契合博彩决策）：
1. 每边偏差分桶（gap = 竞彩隐含 − 欧盘隐含，七档：≤−5 / −5~−2 / −2~−0.5 / ±0.5 / +0.5~2 / +2~5 / ≥+5 pt）。
2. 最大偏离边：竞彩最看好（最大正偏差）/ 最不看好（最大负偏差）/ 分歧最大边。
3. Pinnacle（sharp）倾斜：当 `eu_ps` 存在且比均价更看好某边。

**每个条件**聚合：样本量 n、该边命中率、相对基准 lift、显著性 z（正态近似），以及**用竞彩真实赔率反算的历史 ROI**：
```
ROI = mean( 赢则 (jc_边 - 1), 输则 -1 )
```
这是最诚实的"购买建议"地基——直接回答"按竞彩赔率下这注，历史净收益多少"。

**输出**：按 ROI 排序的规律榜 + 第一公民 `advice`（建议下哪边 / 历史命中% / ROI% / 样本 / 是否"可关注"）。门槛 n≥50，可关注 = ROI>2% 且样本足（z 仅作信息）。

## 二、真实回测结论（2159 场，32ms）

基准命中率 主/平/客 = 41.6% / 26.8% / 31.5%。

### 最高权重的规律（强方向信号）
| 条件 | 边 | 命中率 | z | 历史ROI |
|---|---|---|---|---|
| 竞彩最看好(最大正偏差)边 | 主胜 | 51.8% | **+6.44 显著** | **−13.2%** |
| 竞彩最看好边 | 客胜 | 41.2% | +5.95 显著 | −16.5% |
| 竞彩偏高 +2~5pt | 主胜 | 53.9% | +5.34 显著 | −12.4% |

→ **"竞彩比欧盘明显更看好某边"是该边真的更强**（z 高达 +6.4），是高权重预测特征。

### 但 ROI 全为负 → 市场有效
强信号虽准，赔率已把它定价进去，盲目跟注必亏（−12%~−20%）。这实锤了"市场有效、系统性 edge 不存在"。

### 仅有的 3 个 +EV 局部窗口（薄、偶发，符合预期）
1. **竞彩略低估平局（−2~−0.5pt）→ 下平，ROI +9.3%（n=808，大样本）**
2. 竞彩最不看好边 = 平 → +7.7%（n=465）
3. 竞彩偏低客胜（−5~−2pt）→ +3.6%（n=390）

## 三、给用户的核心结论
- **判断方向**：跟"竞彩最看好边"——它显著更准（z+6.4），用于定方向靠谱。
- **下单盈利**：别盲跟强信号（已被定价）。真能下注的只有那几个边际价值窗口（平局被略低估最值得关注，样本大、ROI 近 +10%）。
- **诚实边界**：历史回测 ≠ 未来；市场有效前提下，这些是"相对最强的局部参考"，不是稳赚模型。

## 四、交付物
- `js/engine/oddsPatterns.js`（引擎，Node+浏览器双挂载，含 `analyze` / `renderPatterns`）
- 集成：index.html 加 `#oddsPatternsPanel` + 脚本；oddsMonitor.js 懒加载缓存 fetch `ml/data/eu_odds_history.json` 渲染；components.css 补面板样式。
- Node 真实回测验证通过；DOM stub 渲染测试通过。

## 五、下一步
- P6 监控预警（连续快照超 `config.alertMove:0.05` → 站内 toast + 邮件接口）。
- P7 展示层（走势图/偏差热力/热度仪表/提醒中心，接 Ardot 设计稿）。
- P8 ML 升级（逻辑回归/XGBoost 替换规则统计，验证是否提升方向准确率）。
