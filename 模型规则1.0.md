# 竞彩智选 Pro · 模型规则 1.0

> 本文档汇总「竞彩智选 Pro」当前（2026-08-11）落地的全部赔率分析规则。
> 规则均从引擎源码（`js/engine/*`）提炼，非凭空设定；阈值、权重、护栏一律标注代码出处。

---

## 0. 核心前提与硬约束

| 约束 | 说明 | 来源 |
|------|------|------|
| **市场有效** | 国际机构赔率 Brier≈0.0005，已高度贴近真实概率；系统性 +EV 不存在 | 回测结论 |
| **固定抽水** | 竞彩固定抽水约 **12.9%**（去水隐含概率 = 1/赔率 / ∑1/赔率） | fundamentals / 各引擎注释 |
| **诚实优先** | 模型只抓局部偶发价值窗口；严禁编造"必胜系统" | 用户硬约束 |
| **advice 第一公民** | 每一层分析必须落地为「买哪边 / 为什么 / 置信度」的可下单建议，不能只给数字 | 用户硬约束（贯穿 P3→P8b） |
| **样本门槛** | 时序分析需 ≥2 个有效快照；规律挖掘需 ≥50 场高置信三件套 | oddsTrend / oddsPatterns |
| **偏差符号约定** | 偏差 = 竞彩隐含概率 − 国际参考隐含；**负 = 竞彩更便宜 = 价值边际** | oddsTrend |

> ⚠️ 历史回测铁证：无脑跟热门 ROI = **−13.01%**（≈抽水，自洽）；价值下注（LR 模型）ROI ≈ **−4.8%**。模型价值 = 纪律性 EV 计算器 / 下注过滤器（少亏约 8pp），**不是制胜系统**。

---

## 1. P3 · 国内外赔率趋势分析（偏差 / 背离 / 收敛）
**模块**：`js/engine/oddsTrend.js` · 无状态纯函数

### 1.1 三类规律定义
- **偏差 Deviation**：当前竞彩隐含 − 国际参考（均值 `intlAvg`，缺则退 Pinnacle `sharp`）。
- **背离 Divergence**：竞彩与国际在一段窗口内反向移动。
  - 竞彩降赔（隐含↑）+ 国际升赔（隐含↓）= 竞彩推、机构撤 → **诱盘预警**（惩罚）。
  - 竞彩升赔（隐含↓）+ 国际降赔（隐含↑）= 机构买、竞彩滞后 → **价值强化**（加成）。
- **收敛 Convergence**：|偏差| 随时间缩小 = 市场趋共识；偏差翻向（crossed）= 最强反转信号。

### 1.2 关键阈值
| 参数 | 值 | 含义 |
|------|----|------|
| `EPS` | 0.005（0.5pt） | 有无变化的判定阈值 |
| `valueThreshold` | **0.03（3pt）** | 触发"建议买"的偏差阈值 |
| `minPoints` | 2 | 最少有效快照数 |

### 1.3 价值分与 action 推导
```
逐边 valueScore = clamp( 偏差相反数(raw) + 形态bonus , -0.15, 0.15 )
  bonus: 偏差在有利方向收敛 → +min(|raw|,0.05)*0.5
         机构买+竞彩撤(强化) → +0.03
         竞彩推+机构撤(诱盘) → -0.04

action:
  valueScore ≥ 0.03        → buy   （建议买）
  0 < valueScore < 0.03     → watch （观望）
  valueScore ≤ 0           → avoid （回避）
```
- **置信度** `conf = clamp( valueScore/0.09 , 0,1 ) × clamp( (样本数-2)/6 , 0.5,1 )`
- **诱盘反向提示**：任意边出现"竞彩推升、国际撤出"且 valueScore<0 → 文案强制提示规避。

---

## 2. P4 · 市场热度指数（语义化）
**模块**：`js/engine/oddsHeat.js` · 无状态纯函数

> 国内竞彩不公开成交量，故用**赔率变动反推资金流向**（不依赖必发交易所）。

### 2.1 热度分合成（逐边 0~100）
```
raw      = 0.45 × 竞彩端漂移(driftN)
         + 0.40 × 机构/聪明钱漂移(sharpN)   // 权重更高
         + 0.15 × 近期加速度(momN)
score    = round( clamp(raw, 0, 1) × 100 )

漂移归一化分母：driftScale=0.08（8pt 封顶）；momentumScale=0.05
```

### 2.2 热度标签
| 分数 | 标签 | 颜色 |
|------|------|------|
| ≥75 | 爆热 | red |
| ≥55 | 热 | amber |
| ≥35 | 温 | blue |
| ≥15 | 偏冷 | muted |
| <15 | 冷 | muted |

### 2.3 advice 动作推导
| 条件 | 动作 | 置信 |
|------|------|------|
| `smartConfirmed`（机构买入且方向与竞彩一致）且 P3 价值 ≥0.03 | **follow 顺势跟随** | 0.5~0.85 |
| `smartConfirmed` 但 P3 无价值 | **follow_small 小注跟随** | 0.35~0.7 |
| `retailOnly`（竞彩单涨、机构未动）且 P3 背离确认 | **fade 反手/回避** | 0.4 |
| `retailOnly` 无 P3 确认 | **caution 警惕诱盘** | 0.35 |
| 最热边恰为 P3 诱盘边 | **fade**（强制覆写） | ≥0.45 |
| 三边均 <15（均衡） | **watch 观望** | 0.2~0.3 |
| 最后一步跳变 >2pt | 附加 ⚡异动提示 | — |

---

## 3. P5 · 权重规律挖掘（历史三件套回测）
**模块**：`js/engine/oddsPatterns.js` · 静态偏差规律

> 历史集每场仅一个国际收盘快照（无时序），故挖**静态偏差**规律（竞彩隐含 vs 欧盘均价），而非 P3/P4 的时序背离。

### 3.1 特征工程
1. **偏差分桶**（七档，单位=隐含概率）：
   `≤-5pt | -5~-2pt | -2~-0.5pt | ±0.5pt | +0.5~2pt | +2~5pt | ≥+5pt`
2. **最大偏离边**：竞彩最看好（最大正偏差）/ 最不看好（最大负偏差）/ 分歧最大边。
3. **Pinnacle sharp 倾斜**（仅当数据含 `ps` 字段时计算）。

### 3.2 统计与排序
- 每条件算：`n / 命中率 wr / 基准命中率 baseWr / 相对基准 lift / 显著性 z / 历史 ROI`。
- **历史 ROI** `= mean( 赢则 jc-1，输则 -1 )`（用竞彩真实赔率反算，最诚实的"购买建议"地基）。
- 排序：**按历史 ROI 降序**；过滤 `n ≥ 50`。

### 3.3 可行动判定
```
actionable = ROI > 2%（0.02）且 n ≥ 50
marketEfficient = (actionable 数量为 0)  // 默认为真 → 市场有效
```
- `significant`：|z| ≥ 1.96。
- advice 仅展示 `ROI > 2%` 的局部窗口（薄、偶发），并标注 n / z / lift，防过拟合吹水。
- **结论**：最高权重规律 = "竞彩比欧盘明显更看好某边" 该边确实更准（如最大正偏差主胜命中 51.8%，z=+6.44 显著）；但所有规律 ROI 全负 → 信号已被定价，盲目跟注不赚钱。

---

## 4. P6 · 赔率异动监控与提醒
**模块**：`js/engine/oddsAlert.js` · 监控 + 交叉验证

### 4.1 阈值
- 异动阈值 `CONFIG.thresholds.alertMove = 0.05`（**5 个隐含概率点**），见 `config/config.js`。
- 全市场扫描窗口：最近 **10 分钟**内的异动。

### 4.2 三件套职责
| 函数 | 用途 |
|------|------|
| `detectMovements(series)` | 单场：扫描全部快照异动 + 下建议（赔率监测页卡片） |
| `detectFromOdds(prev,cur)` | 轮询点：前后两次赔率直接判定（零额外 IO，用于全局 toast） |
| `scanAll(seriesMap)` | 全市场：扫描所有比赛最近异动（提醒中心） |

### 4.3 advice 交叉验证推导
- `direction`：`impDeltaPp > 0` = 赔率降 = **资金流入(in)**；`< 0` = 流出(out)。
- 与 P3 趋势、P4 热度交叉验证：
  | 情形 | 动作 |
  |------|------|
  | 流入 + P3 诱盘（竞彩推/国际撤） | **fade 反手/回避** (0.45) |
  | 流入 + 仅竞彩单热（retailOnly） | **caution 警惕诱盘** (0.35) |
  | 流入 + 机构确认 / P3 价值≥0.03 | **follow 顺势关注** (0.45~0.85) |
  | 流入但机构未确认 | **watch 观望** (0.3) |
  | 流出（资金撤离） | **fade 反手/回避** (0.4) |
  | P3 该边本就"买" + 流入 | **follow** 强制覆写 (≥0.6) |
- 提醒通道：站内 toast（复用 `#toastContainer`，9s 自动消失）+ 邮件接口预留（`setEmailHook`）+ localStorage 日志（最多 100 条）。

---

## 5. P8 · ML 建模（多源融合逻辑回归）
**模块**：`js/engine/oddsModel.js` · 前端可移植推理（系数内联，无需网络）

### 5.1 模型
- 类型：多项逻辑回归（multinomial logistic regression）。
- 特征（scheme B，9 维）：`jc_h/d/a`（竞彩隐含）+ `eu_h/d/a`（欧盘均价隐含）+ `gap_h/d/a`（竞彩−欧盘偏差）。
- 训练集：2159 场高置信三件套（`ml/p8_train.py` 导出 `p8_model_lr.json`，系数内联兜底）。
- 推理：`softmax( intercept + Σ coef·feature )` → h/d/a 概率。

### 5.2 每边 EV 计算（第一公民 advice）
```
breakeven = 1 / 竞彩赔率           // 含抽水
EV_side   = 模型概率 p - breakeven  // >0 即理论价值
```

### 5.3 合理性护栏（关键）
```
MAX_EDGE_RATIO = 1.6
saneBest = p[bestIdx] ≤ 1.6 × breakeven[bestIdx]
hasValue = (EV > 0) AND saneBest   // 防止模型在极端盘过度自信出"假价值"
```
- 置信度：`hasValue ? min(0.95, 0.5 + bestEV×3) : p[idx]`。
- 动作：`hasValue → bet`；`-0.01 < EV ≤ 0 → watch`；`EV ≤ -0.01 → fade`。
- 无价值时只给方向参考，并明示"回测价值下注 ROI≈−4.8%"。

### 5.4 全量逐场排名（P8b 前置）
- `scoreHistory(history)` 对候选池 2159 场全量打分。
- 排名表支持「概率优先 / EV 优先」排序切换；默认前 150 场，可一键展开全部。
- 每行：三向模型概率（推荐边加粗）/ 推荐边 / 竞彩赔率 / 盈亏平衡 / EV(pp) / 类型标签（价值 / 非价值 / 方向）。

---

## 6. P8b · 串关（Accumulator）推荐引擎
**模块**：`js/engine/oddsParlay.js` · 依赖 `OddsModel.scoreHistory`

### 6.1 组合公式（竞彩串关赔率相乘）
```
组合概率  P = ∏ p_i
组合赔率  O = ∏ jc_odds_i
组合 EV   = P×(O−1) − (1−P)
```

### 6.2 两套策略（明确的风险/回报取舍，非必胜系统）
| 策略 | 选腿逻辑 | 目标 |
|------|----------|------|
| **稳妥型 stable** | 每腿取模型概率最高的边，挑 top-N 场（不同比赛） | 最大化组合命中率，赔率偏低 |
| **冷门型 cold** | 理性冷门区间（**赔率 2.5~8×**）内优先取 EV 为正的边，按 EV 降序 | 潜在回报高，命中率低 |

### 6.3 冷门型护栏（与 P8 同源）
```
MAX_EDGE_RATIO = 1.6
纳入条件：模型概率 p ≤ 1.6 × 盈亏平衡(1/jc)
        且 2.5 ≤ jc ≤ 8
→ 剔除 10×+ 极端长 shots 与模型过度自信的"彩票式假价值"
```
- 若区间正 EV 不足 N 条 → 退到区间内全部（仍受护栏）；再不足 → 退回全量按 EV 降序。
- 面板如实标注：组合 EV 为样本内拟合（乐观），P8 外推回测显示真实 ROI≈−4.8%，定位为"价值倾向候选"而非已验证优势。

---

## 7. recommend.js · 价值信号闸门（双路径）
**模块**：`js/engine/recommend.js`

| 路径 | 条件 | 价值定义 | 标记闸门 |
|------|------|----------|----------|
| 路径1（金标准） | 有实时欧盘 | V = 欧盘隐含 − 竞彩隐含（>0 = 竞彩更优） | (jc−eu) < 历史 p10 → 竞彩显著便宜 |
| 路径2（无欧盘） | key-free，用 `eu_jc_gap.json` 历史先验 | 模型价值边际 = 校准概率 − 竞彩去水隐含 | **模型价值 ≥ +5%** 才标记；若同时 > 该联赛历史 p90 → **极强（💎💎）** |

- 联赛样本 <40（n<40）则回退全局 ALL 分布。
- 联赛级偏差仅作"假设"，需实时验证，不单独作为下注依据。

---

## 8. 统一 advice 动作字典

| 动作 | 含义 | 典型触发 |
|------|------|----------|
| `buy` / `bet` | 建议买（有明确价值窗口） | P3 偏差≥3pt；P8 EV>0 且过护栏 |
| `watch` | 观望（略有价值或均衡） | 偏差 0~3pt；机构未确认热度；无异动 |
| `avoid` / `fade` | 回避 / 反手 | P3 价值≤0；P4/P6 诱盘或资金流出 |
| `follow` | 顺势跟随 | 机构+竞彩一致流入；P3 价值边异动强化 |
| `follow_small` | 小注跟随 | 机构确认但 P3 无价值 |
| `caution` | 警惕诱盘 | 仅竞彩单热、机构未动 |

---

## 9. 诚实结论与边界

1. **系统性 +EV 不存在**：市场有效（Brier≈0.0005），信号已被定价；所有规律 ROI 全负，盲目跟注不赚钱。
2. **模型定位**：纪律性 EV 计算器 / 下注过滤器（价值下注 ROI≈−4.8%，远优于无脑下注 −13%），**非制胜系统**。
3. **数据边界**：
   - 实时国际赔率仅 The Odds API（免费 500/月），国内直连受限 → 需 SCF 代理或 `?direct`；国际采集默认关闭（opt-in `recordIntl`）+ 节流（15min）保命配额。
   - 情报矩阵（伤病/天气/裁判）无实时数据源 → 模型估算 + "赛前核对"占位，不编造。
   - 历史三件套高置信 2159 场（25/26 欧洲赛季），欧冠/欧协等冷门联赛覆盖不足。
4. **所有建议均标注置信度与来源**，仅供参考，不构成稳赚承诺；竞彩固定抽水约 12.9%。

---

*文档版本：模型规则 1.0 · 生成日期 2026-08-11 · 与 `js/engine/*` 源码一致*
