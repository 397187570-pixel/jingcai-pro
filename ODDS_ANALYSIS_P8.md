# P8 · ML 建模升级（逻辑回归 / XGBoost 替换规则统计）

> 目标：用监督学习模型替换 P5 的「静态偏差规则统计」，并严格验证
> **在方向准确率与价值下注 ROI 上是否真的带来增量价值**。

> 结论先行：**市场有效，系统性 +EV 不存在**。ML 模型（逻辑回归）在方向准确率上
> 与「总是选热门 / 市场隐含概率」基线持平，树模型反而过拟合；价值下注回测 ROI
> 仍为负（−4.8%，但远优于无脑下注的 −13%）。模型的正确定位是
> **纪律性 EV 计算器 / 下注过滤器**，而非制胜系统——这与 P5 结论互相印证，
> 并用量化模型把"为什么没有 edge"坐实了。

---

## 1. 方法与数据

- **数据**：`ml/data/eu_odds_history.json` 高置信三件套 2159 场（竞彩赔率 + 欧盘均价 +
  赛果，单赛季 25/26）。
- **特征**（均可由"竞彩赔率 + 欧盘均价"实时推导，便于移植前端）：
  - 方案 A（6）：竞彩隐含 + 欧盘均价隐含
  - 方案 B（9）：+ 偏差(竞彩−欧盘) ← **采用**
  - 方案 C（15）：+ 必发365隐含 + 偏差
- **模型**：多分类逻辑回归（可无损移植 JS） + 梯度提升
  （`HistGradientBoostingClassifier` 作 XGBoost 代理；环境缺 `libomp`，真 XGBoost 无法
  在沙箱运行，但 Boosting 树在 2159 样本上行为与代理一致，结论不受影响）。
- **防泄漏**：价值下注 ROI 全部用 5-fold **Out-of-Fold (OOF)** 预测计算。
- **价值下注逻辑**：对每场，仅当模型概率 `p > 1/jc_o`（赔率盈亏平衡，含竞彩 12.9% 抽水）
  才下注该边（取 edge 最大边）；stake=1，ROI = 总利润 / 下注次数。

---

## 2. 结果

### 2.1 基线（合理性校验）
| 基线 | 指标 |
|---|---|
| 总是选热门 acc | **0.5039** |
| 市场隐含(jc_implied) logloss / brier | 1.0085 / 0.6035 |
| 无脑下热门 ROI | **−13.01%**（恰好 = 竞彩抽水，完美自洽 ✓） |

### 2.2 模型（5-fold OOF）
| 方案 | 模型 | acc | logloss | brier | 价值下注 ROI |
|---|---|---|---|---|---|
| A(6) | LR | 0.5039 | 1.0080 | 0.6031 | −7.00% |
| **B(9)** | **LR** | **0.5030** | **1.0081** | **0.6032** | **−4.84%** |
| C(15) | LR | 0.5021 | 1.0081 | 0.6032 | −9.10% |
| A | GBM | 0.4697 | 1.0765 | 0.6406 | −10.27% |
| B | GBM | 0.4632 | 1.0858 | 0.6443 | −5.63% |
| C | GBM | 0.4627 | 1.0909 | 0.6484 | −6.94% |

### 2.3 关键解读
1. **方向准确率**：LR(0.503) ≈ 总是选热门(0.504) ≈ 市场隐含。逻辑回归只是重新发现了
   "市场隐含概率就是最好的方向信号"。**GBM 全部劣于基线（0.46~0.47）**——
   2159 样本上 300 棵树严重过拟合。→ ML 不提升方向判断。
2. **概率校准**：LR logloss 1.0081 vs 市场 1.0085，仅噪声级差异，无实质改进。
3. **特征重要性**：LR 系数中 `jc_*` 与 `eu_*` 同时显著（双源融合正确），偏差主要对
   「平局」有信息量；HistGBM 排列重要性 top = `eu_d / eu_a / gap_h / jc_d`——
   国际共识 + 偏差是真正的信息源，但仍打不穿市场。
4. **价值下注 ROI**：无脑 −13.01%（=抽水）；LR 价值筛选 **−4.84%**——比无脑少亏约 8pp，
   但**仍为负**。说明模型的价值筛选能屏蔽最差的 −EV 下注，却创造不出 +EV。

---

## 3. 诚实结论

- **不存在系统性 +EV edge**。任何"下注系统"在有效市场上长期必亏（≈抽水）。
- 逻辑回归的价值在于：把 P5 手工写的"偏差规则"升级为**数据驱动、可校准、可解释**的
  概率模型，并提供一个**纪律性 EV 计算器**——告诉你哪边 EV 最高、是否 >0，避免情绪化
  追最差赔率。
- 实施建议：模型**保留并接入**，但 UI/建议文案必须**如实标注"非价值 / 仅方向参考"**，
  不得暗示稳赚。这与用户硬约束（分析必须给可下单建议，但不得编造价值）一致。

---

## 4. 交付物

| 文件 | 说明 |
|---|---|
| `ml/p8_train.py` | 训练 / 交叉验证 / 价值下注 ROI 回测脚本（可复跑） |
| `ml/p8_model_lr.json` | 导出的最优 LR 模型（scheme B，9 特征，系数+截距） |
| `js/engine/oddsModel.js` | **前端可移植推理模块**：系数内联（无需网络），`predict()` + `advice()`，含每边 EV |
| `index.html` | 新增 `#oddsModelPanel` 容器 + `oddsModel.js` 脚本 |
| `js/components/oddsMonitor.js` | 新增 `renderModelPanel()`，与 P5 面板并列渲染 |

### JS 移植正确性校验
- 内联系数与 `p8_model_lr.json` **bit 一致**（已断言）。
- 对同一场比赛，JS 推断概率与 Python **逐场完全一致**（如 比利亚雷 vs 马竞
  p=[35.99, 29.77, 34.25]；AC米兰 vs 卡利亚里 p=[73.93, 16.35, 9.72]）。

### `advice` 层（满足"必须给买哪边建议"硬约束）
- 有正 EV → `action:'bet'`，标注"价值窗口：倾向下X" + EV(pp) + 置信。
- 无 +EV → `action:'watch'/'fade'`，明确"无 +EV 窗口，模型倾向X（仅方向参考）" + 回测 ROI 警示。
- 每边输出模型概率 + 盈亏平衡 + EV，透明可复核。

---

## 5. 已知限制 / 后续
- **XGBoost 未实测**（沙箱缺 `libomp`）。若需在真机跑：装 `brew install libomp` 后
  `pip install xgboost`，脚本已内置 XGBoost 分支（预期结论不变：树模型过拟合）。
- **样本量天花板**：2159 场单赛季。要进一步，只能扩充历史赛季（需对应竞彩赔率，见 P5 数据扩充）。
- **实时化**：当前模型用收盘静态快照训练；接入 P3 时序后，可把"偏差随时间变化"作为动态
  特征重训（届时方向/价值可能略有改善，但市场有效前提下期望仍接近 0）。
- 与 P5 关系：P8 不替代 P5 的"规则榜"展示，而是提供**模型概率/EV 维度**作为并列参考。
