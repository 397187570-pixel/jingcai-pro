# P4 · 市场热度指数（语义化）落地说明

> 竞彩智选 Pro · 数据分析系统
> 日期：2026-08-07

## 一、为什么是"语义化"

我们核查过（见对话）：**现在能拿到的真实成交量基本为零**。
- 手头 1157 场历史 + 实时数据：都只有赔率，无 `volume` / 投注量字段。
- 国内竞彩官方从不公开任何成交量/投注量。
- 唯一真金白银的成交量在 **Betfair/必发交易所**（`totalMatched`），但国内被墙 + 需密钥，只能做 **opt-in 升级**（沿用 `recordIntl` 模式 + 海外中转）。

因此 P4 默认**不依赖成交量**，改用"赔率变动"反推钱往哪边挤：

| 信号 | 含义 | 权重 |
|---|---|---|
| 竞彩端赔率被压低（隐含概率↑） | 散户 / 跟单热度升 | 45% |
| 机构端（国际均值 / Pinnacle）赔率被压低 | 聪明钱买入 | 40%（更高） |
| 近期变动加速度（最后 1/3 段 − 早期段） | 升温 / 降温 | 15% |

合成为每边 **0~100 热度分** + 冷/偏冷/温/热/爆热 标签。

## 二、下单建议（advice，用户硬约束）

热度本身不直接等于"该买"——必须交叉验证才能落地成建议：

| 局面 | 动作 | 置信 |
|---|---|---|
| 机构确认买入 + P3 检出价值 | **顺势跟随** follow | 高（0.5~0.85） |
| 机构确认买入，但无价值 | **小注跟随** follow_small | 中（0.35~0.7） |
| 仅竞彩端推升、机构未确认 | **警惕诱盘** caution | 中 |
| 最热方向恰是 P3 诱盘边（竞彩推/国际撤） | **反手/回避** fade | 中高 |
| 三边都冷 | **观望** watch | 低 |

> `analyze()` 的输入可传 `trendResult`（P3 结果），自动做价值交叉验证——两个面板共享同一份时序，零额外成本。

## 三、文件与集成

- 新增 `js/engine/oddsHeat.js`：纯函数引擎（浏览器/Node 同构），导出 `analyze / renderHeat / heatLabel`，双挂载 `window.OddsHeat` + `module.exports`。
- `index.html`：赔率监测页新增容器 `#oddsHeatPanel`；在 `oddsTrend.js` 后引入脚本。
- `js/components/oddsMonitor.js`：`renderOddsMonitor` 改为**一次** `OddsHistory.getSeries(code)` → 先算 P3 趋势 → 结果传给 P4 热度 → 分别渲染两张卡片；无数据 / 异常均优雅降级，不破坏既有静态 UI。

## 四、验证

Node 冒烟测试 5 场景全过：
1. 聪明钱确认 + 主胜价值 → `follow`，置信 0.81 ✅
2. 仅竞彩推主胜 + P3 诱盘背离 → `fade`，置信 0.45（headline 已修正为"疑似诱盘→反手"）✅
3. 三边均衡 → `watch` ✅
4. 最后一步跳变 >2pt → `spike:true`（供 P6 预警复用）✅
5. 不足样本 → `insufficient`，优雅提示 ✅

渲染 DOM stub 测试：正常态 / 不足态均产出 HTML 且不抛错。

## 五、下一步（P5–P8）

- **P5 权重规律挖掘**：用 1157 场「国内+国外+赛果」三件套回测，把 P3/P4 的偏差/背离/收敛/热度当作特征，做规则统计显著性 + 权重排序。
- **P6 监控预警**：复用 `config.alertMove:0.05` + P4 的 `spike` 检测 → 连续快照超阈值触发站内 toast + 邮件接口预留。
- **P7 展示层**：走势图 + 偏差热力 + 热度仪表 + 提醒中心（可接 Ardot 设计稿）。
- **P8 ML 建模升级**：逻辑回归 / XGBoost。

> 注：Ardot 设计适配器本次会话多次重试仍连不上，展示层（P7）的可视化设计稿待适配器恢复后再补；工程地基已先行落地。
