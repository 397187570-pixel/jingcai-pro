# 三件套数据集扩充（P5 前置工作）

> 用户诉求：「开工前你评估是否需要接入更多场次的三件套？有价值的话先做这个工作」

## 一、评估结论

**需要、且已扩充。价值明确：免费、本地、低风险，把训练样本翻倍。**

### 数据天花板定位
- 竞彩历史 `ml/data/history_matches.json` = **仅 25/26 单赛季（4399 场）**。
  → 三件套的「国内赔率」这条腿天然封顶在 25/26，无法靠下更多历史赛季突破
  （football-data.co.uk 沙箱可直连，但缺对应赛季的竞彩赔率，凑不出三件套）。
- 真正瓶颈是 **匹配率**，不是数据来源：
  - CROSSWALK 的 17 个欧洲联赛在竞彩里有 2544 场，原 `build_eu_history.py` 只匹配上 1157（≈45%）。
  - 根因有二：(1) 单遍在线 bootstrapp 别名，排序问题漏配；(2) **竞彩中国日期 vs football-data 英国日期差 1 天**，晚场/凌晨比赛直接 join 失败。

### 诊断证据（英超抽样）
| 指标 | 数值 |
|---|---|
| 竞彩英超有效场次 | 304 |
| 精确同日命中 | 154 |
| **需日期±1 偏移才命中** | **121（40%！）** |
| 含±1天可匹配 | 275 / 304（90%） |
| 别名缺失 / 比分分歧 / fd缺失 | 0 / 0 / 29 |

## 二、做了什么

新增 `ml/build_eu_history_v3.py`：
1. **两遍匹配**：第一遍全量 bootstrapp 中文↔英文队名别名；第二遍再用别名精确 join。
2. **日期±1 天容差**：精确 join 允许 `(联赛代码, 日期±1, 英文队名)` —— 用「队名 + 比分」双重锁定防错配。
3. **置信标签 `match_method`**：`exact` / `exact_offset` / `score_unique` / `score_unique_offset` / `low_conf`。P5 回测只取非 low_conf。

顺带修复：`ml/data/eu_raw/N2.csv` 是损坏的错误页（football-data 25/26 未发布荷乙），已删除。

> 注：曾试 `build_eu_history_v4.py`（全局别名跨赛事复用），对欧冠无帮助且引入跨联赛重名风险，已弃用，最终采用 v3。

## 三、成果（对比）

| 指标 | 扩充前 | 扩充后 |
|---|---|---|
| 三件套总场次 | 1157 | **2225（50.6%）** |
| **高置信（非 low_conf）** | 1082 | **2159（翻倍）** |
| 意甲 / 西甲 / 英超 | ~46-62% | **100%** |
| 德甲 / 法甲 / 荷甲 / 英冠 | ~58-75% | **98-99%** |
| 葡超 / 法乙 / 英甲 | ~20-75% | **98-100%** |

质量校验：国际赔率（eu_avg/eu_b365）零缺失；**竞彩比分 vs 欧盘比分一致性 99.91%**（2 条不一致恰为 low_conf，P5 排除）。

同时重新生成 `eu_jc_gap.json`——它作为 `recommend.js` 的「国内vs国外偏差」历史先验，样本翻倍后更准，线上推荐引擎自动受益。

## 四、残余短板（可选增强，非阻塞）
- 欧冠 EC 21/171（12%）、欧协 ECL 33%、英锦 E3 0%：疑为 CL 比赛日期错位超 1 天或 EC 文件结构差异，需手动补别名或放宽容差，收益有限（共 ~216 场）。
- 荷乙 N2 0%：数据源 25/26 未发布，无解。

## 五、对 P5 的意义
2159 场高置信三件套（带 竞彩隐含 / 欧盘均价隐含 / 赛果）足以：
- 做**诚实的 train/test 切分**回测（不再样本吃紧）。
- 按联赛细分权重，降低过拟合。
- 统计「偏差/背离类特征」对赛果的显著性，输出带置信度的规律与下单建议。
